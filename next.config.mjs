/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "honorable-chipmunk-576.convex.cloud",
      },
    ],
  },
};

export default nextConfig;
